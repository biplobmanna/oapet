# Basically a script to open any project file 
* should have search function
* should have fuzzy search
* should have options to choose any of the available options
* should have a config file to alter parameters
